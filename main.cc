#include <iostream>
#include "floor.h"
using namespace std;

int main (int argc, char *argv[]) {
    const int width = 79;
    const int height = 25;


    Floor f{width, height};

    if (argc == 2) { // optional cmd line arg

    } else {

    }
}
